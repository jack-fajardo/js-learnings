const nested = [[1, 2], [3, 4], [5]];

const flattenThenSum = (nestedArr) => {
  const flatenned = nestedArr.reduce((acc, val) => {
    acc.push(...val);
    return acc;
  }, [])

  return flatenned.reduce((a, b) => a + b, 0)
}

console.log(flattenThenSum(nested))